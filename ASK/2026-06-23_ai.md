# 2026-06-23 Claude/Codex 질문·응답 로그

> 작성 규칙: 질문마다 한 블록. 질문 한 줄 요약 + 응답 2~3줄 요약.
> 도구 구분: `Claude`, `Codex`, 또는 `Claude+Codex`.

---

## KIBA → vibe_2026 마이그레이션 스크립트 오류 수정
**도구:** Claude
**질문:** migrate_to_vibe_2026.ps1 실행 중 New-ScheduledTaskAction 'Execute' 인수 null 오류가 났다.
**응답 요약:**
- 모든 예약 작업을 재생성하던 로직이 Exec 아닌 action(빈 Execute)에서 실패한 원인을 찾아, 기존 action 객체를 제자리 치환하도록 수정했다.
- 후행 백슬래시 없는 WorkingDirectory(`...\Desktop\KIBA`)가 누락되던 버그를 경계 인식 정규식으로 고치고, 살아있던 KIBA 예약 작업 4개의 옛 경로를 모두 새 경로로 보정했다.
- 사전 점검을 idempotent하게 바꿔 이미 이동된 상태에서도 재실행 가능하게 했다.

---

## Knowledge 로컬 링크 절대경로 → 상대경로 전환
**도구:** Claude
**질문:** 재생성된 Knowledge 파일들을 어떻게 해야 하나(절대경로 때문에 이동·기기변경마다 깨짐).
**응답 요약:**
- `build_issue_knowledge.py`의 `file_link()`를 머신 절대경로(`file:///C:/...`) 대신 노트 위치 기준 상대경로로 생성하도록 변경하고, 호출부에 노트 폴더를 전달했다.
- Knowledge/·wiki/를 재생성(절대경로 0건 확인)하고 커밋·푸시했다.

---

## git push 실패 — 전역 .gitconfig 옛 경로 보정
**도구:** Claude
**질문:** (push 시도 중) credential helper가 옛 gh.exe 경로를 가리켜 인증 실패.
**응답 요약:**
- `~/.gitconfig`의 credential helper(github/gist)와 safe.directory가 옛 `Desktop\KIBA` 경로를 가리키던 것을 발견, `gh auth setup-git`(새 gh.exe)과 수동 보정으로 새 경로로 갱신했다.
- 이후 push 성공, 옛 경로 참조 0건 확인.

---

## 마이그레이션 스크립트에 .gitconfig·Obsidian 경로 보정 단계 보강
**도구:** Claude
**질문:** 다음 이전을 대비해 스크립트에 .gitconfig 경로 치환 단계를 추가하고, 다른 설정도 점검해 달라.
**응답 요약:**
- migrate 스크립트에 전역 .gitconfig 경로 보정 단계(백슬래시/슬래시 두 표기, 경계 매칭, 백업) 추가. 구문 검사·합성 데이터 단위 테스트 통과.
- 추가 점검 결과 Obsidian `obsidian.json`의 vault 경로만 유효한 살아있는 포인터로 판단, JSON 파싱으로 해당 path만 보정하는 단계도 추가(.claude.json·.codex는 과거 기록이라 제외).

---

## db-tables 비밀번호 게이팅(실제 값·다운로드) 구현
**도구:** Claude
**질문:** db-tables.html에 비밀번호 기능 추가 — 미입력 시 스키마만, 입력 시 테이블 값·다운로드. 비밀번호는 메인 문서 다운로드와 동일하게.
**응답 요약:**
- 배포본은 정적 스키마 페이지였고 로컬엔 미커밋 Worker 재작성본이 있어, 정적 스키마 페이지를 기반으로 비밀번호 게이팅(값 조회·테이블별/전체 다운로드)을 얹었다. 미커밋 재작성본은 stash로 보존.
- 백엔드 Worker `/db/tables`·`/db/table`·`/db/download`(모두 DOCS_PASSWORD+repo 검증)를 함께 커밋·배포하고, 라이브에서 게이팅(403 bad_password) 검증.

---

## Worker DB_TABLES 파일명을 실제 R2 객체명으로 정정
**도구:** Claude
**질문:** R2(kiba-docs-private/db/)에 CSV는 있는데 실제 값이 안 뜬다(버킷 통일 선택).
**응답 요약:**
- Worker가 찾는 키(`01_직원.csv` 등)와 실제 R2 객체명이 달라서 실패하던 것을 확인, 실제 업로드명(`db_직원기본정보.csv` 등 `db_` 접두사 포함)에 맞춰 DB_TABLES source를 정정·재배포했다.
- 데이터 버킷은 kiba-docs-private로 통일하기로 결정.

---

## 실제 값 표 10행 고정 + 내부 스크롤
**도구:** Claude
**질문:** 값 표를 10행으로 고정하고 나머지는 표 내부 스크롤로, 헤더 고정·스크롤바도 보기 좋게.
**응답 요약:**
- 값 표를 헤더+정확히 10행 높이로 고정(행 높이 측정)하고 내부 세로 스크롤, thead sticky, 스크롤바 스타일·overscroll 처리 추가. 검색 필터에도 동일 적용.

---

## 협회등록현황 xlsx 추가(스키마·값·다운로드)
**도구:** Claude
**질문:** db_협회등록현황_2026-06-19.xlsx도 스키마 만들고 화면에 표시·다운로드. 스키마는 지정한 13개 컬럼으로.
**응답 요약:**
- xlsx라 Worker CSV 파서 대신 브라우저 SheetJS로 파싱해 표시, `/db/download`는 확장자·Content-Type 보정해 원본 엑셀 제공하도록 했다.
- 스키마는 지정한 13개 컬럼(보험·이름·소속·직책·명함·출입증·방위사업 신원조사·각 협회 기존/신규)으로 정적 표시(비밀번호 없이 노출), 실제 값·엑셀 다운로드는 비밀번호 뒤로.

---

## Quartz(quartz_kiba)에 협회 등록 현황 반영 및 데이터 페이지 통일
**도구:** Claude
**질문:** quartz_kiba 사이트에도 반영하고, 다른 6개 데이터 페이지에도 실제 값·다운로드 링크를 통일해 달라.
**응답 요약:**
- `content/quali-fit-data/`에 협회 등록 현황 페이지(13컬럼 스키마 + 보호된 DB 보기 링크)를 추가하고 index 구성표·관계도에 연결.
- 기존 6개 데이터 페이지(employee/certificates/work-codes/education/employee-certs/work-cert-map)에 동일한 "실제 값·다운로드" 링크 섹션을 추가, v5 push로 배포·라이브 확인.
