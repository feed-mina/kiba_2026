# 2026-06-27 AI ASK 로그

## sample_ver1 원가계산서 DB설계 이슈 생성
**도구:** Codex
**질문:** sample_ver1 원가계산서 시트를 DDD 방식 DB로 만들기 위해 새 이슈를 만들고, GitHub Project와 진행 UI/Todo에 보이게 하며, 설계에 필요한 객관식 질문 4개와 답변 기반 DB 스키마를 작성해달라.
**응답 요약:**
- `(E)sample_원가계산보고서ver1.xlsx.xlsx` workbook의 핵심 시트와 수식 흐름을 확인했다.
- `단가대비표 -> 일위대가표 -> 일위대가목록/내역서 -> 집계표 -> 원가계산서 -> 결과` 흐름과 보험료/경비/일반관리비/이윤 보조 산출표를 DDD 경계로 정리했다.
- `Todo/2026-06-27_sample_ver1_cost_sheet_db_design.md`에 객관식 설계 질문 4개, 선택 답변, PostgreSQL DDL 초안을 작성했다.

## sample_ver1 DB설계 Q2/Q3 D버전 추가
**도구:** Codex
**질문:** DDD 경계 결정 중 Q2와 Q3에 추가 D버전을 넣고 이어서 진행해달라.
**응답 요약:**
- Q2에 `FormulaPolicy` 버전을 추가해 Excel 원문 수식, 파싱 AST, DB 계산식, 적용 버전을 정책 객체로 관리하도록 보완했다.
- Q3에 `SheetLineProjection` 버전을 추가해 canonical `CostLine`과 원본 Excel 행/셀 표시 구조를 분리하도록 보완했다.
- PostgreSQL DDL에 `calculation_policy`, `sheet_line_projection`, `formula_definition.policy_id`를 추가하고 GitHub Issue #40 본문도 갱신했다.

## sample_ver1 importer/schema 실작업 초안
**도구:** Codex
**질문:** 이슈 #40을 이어서 진행해달라.
**응답 요약:**
- `scripts/import_sample_ver1_cost_workbook.py`를 추가해 `sample_ver1` Excel workbook의 시트, 셀, 수식, 의존성, golden cell을 raw XLSX XML 기준으로 추출하도록 했다.
- `data/sample_ver1_cost_db/schema.sql`에 DDD 기준 PostgreSQL 스키마 초안을 작성하고, `README.md`와 `workbook_manifest.json` 산출물을 추가했다.
- importer 실행 결과 `sheets=24`, `formulas=595`, `dependencies=527`, `warnings=243`을 확인했다. 주요 warning은 빈 수식 242건과 깨진 참조 1건이다.
- Todo 체크리스트에서 importer, DDL, dependency parser, golden manifest 작업을 완료로 표시했다.

## sample_ver1 golden value 검증
**도구:** Codex
**질문:** 이슈 #40을 이어서 진행해달라.
**응답 요약:**
- `scripts/verify_sample_ver1_golden_values.py`를 추가해 Excel 캐시값과 DB 후보 계산값을 비교하는 golden 검증을 만들었다.
- `workbook_manifest.json`의 formula/golden cell 항목에 `cached_value`와 `cached_value_type`을 추가했다.
- `data/sample_ver1_cost_db/golden_value_check.json` 기준 `원가계산서!E34 = 123,387,460`, `결과!J10 = 123,387,460`이며 두 값 모두 Excel 캐시값과 DB 후보 계산값이 일치한다.
- Excel의 `MIN(D7,H7,J7)` 같은 수식에서 빈 셀은 0으로 계산하지 않고 무시해야 한다는 규칙을 검증 스크립트에 반영했다.

## sample_ver1 총액 테이블 및 db-tables 표시
**도구:** Codex
**질문:** 총액에 관련된 부분을 수정하고 `db-tables.html`에서도 보이게 해달라.
**응답 요약:**
- `schema.sql`에 `cost_total_component`, `cost_total_check`를 추가해 총액 중간값과 최종 검증쌍을 DB에서 추적할 수 있게 했다.
- `db-tables.html` 상단에 `sample_ver1 원가계산서 DB 설계 · 총액 검증` 패널을 추가하고 `원가계산서!E34`, `결과!J10`의 123,387,460 일치 결과를 표시했다.
- `db-tables.html`의 테이블 보기와 스키마 CSV export에도 `sample_ver1_cost_db` 20개 테이블 묶음을 추가했다.

## sample_ver1 일위대가 ETL 회귀 수정
**도구:** Codex
**질문:** 일위대가 ETL 진행 중 `cost_line`이 56행에서 1행으로 줄어든 오류를 수정하고 이어서 진행해달라.
**응답 요약:**
- `extract_cost_line()` 내부의 `return`이 반복문 안으로 들어가 첫 데이터 행 이후 즉시 종료되던 회귀를 수정했다.
- `extract_sample_cost_domain.py`에 기대 행 수 검증을 추가해 `cost_line=56`, `unit_cost_item=4`, `unit_cost_component=15`가 깨지면 실패하도록 했다.
- `unit_cost_component`에서 합계열이 비어 있어도 재료/노무/경비 금액 합으로 `total_amount`를 보강해, 일위대가 항목별 합계와 구성 행 합계를 일치시켰다.
- `db-tables.html`의 sample_ver1 상태를 실제값 `10/20`으로 갱신하고 `unit_cost_item`, `unit_cost_component`를 실제값 테이블로 표시했다.

## 3개 입력 엑셀 기반 원가계산서 생성기 이슈 생성 및 MVP
**도구:** Codex
**질문:** sample1, sample2 형식을 참고해 `단가대비표`, `내역서`, `집계표` 총 3개의 Excel 파일을 넣으면 `원가계산서` Excel이 나오도록 새 이슈를 만들고 작업해달라.
**응답 요약:**
- GitHub Issue #41 `3개 입력 엑셀 기반 원가계산서 생성기 구축`을 만들고 `todo` 라벨을 적용했다.
- `scripts/build_cost_statement_workbook.py`를 추가해 세 입력 workbook에서 `단가대비표`, `내역서`, `집계표`를 찾아 템플릿 workbook에 주입하고, `원가계산서`/`결과` 수식 체인을 유지한 출력 XLSX를 만들도록 했다.
- sample_ver1 round-trip 생성 결과 `원가계산서!E34 = 123,387,460`, sample_ver2 round-trip 생성 결과 `원가계산서!E34 = 109,104,460`으로 golden 검증을 통과했다.
- `Todo/2026-06-27_cost_statement_generator_from_three_inputs.md`와 `data/cost_statement_generator/README.md`에 사용법, 검증값, 다음 작업을 기록했다.

## 원장님용 원가계산서 생성 UI 반영
**도구:** Codex
**질문:** `quartz-kiba.pages.dev`와 `feed-mina.github.io/kiba_2026`에서 캡처 이미지처럼 `단가대비표`, `내역서`, `집계표`를 불러오면 `원가계산서`를 받는 기능을 원장님이 쓰기 쉬운 UI로 만들어달라.
**응답 요약:**
- `index.html` 상단에 `원가계산서 만들기` 패널을 추가해 `불러오기` 3칸과 `다운로드` 결과 영역이 한눈에 보이도록 했다.
- 3개 Excel 파일 선택 상태를 브라우저에서 확인하고, sample1/sample2 결과 workbook 다운로드 링크와 Issue #41 링크를 배치했다.
- `quartz_kiba` 생성 스크립트에 #40/#41과 `엑셀생성` 활동을 추가하고, `content/notes/cost-statement-generator.md` 설명 노드를 만들어 Quartz 그래프에서 연결되도록 했다.
- GitHub 라벨 추가는 사용량 제한으로 수행하지 못해, 로컬 생성 스크립트에서 #40/#41을 명시 공개 업무로 포함하는 방식으로 처리했다.

## 원가계산서 생성 요청 API 연결
**도구:** Codex
**질문:** API 연결 및 GitHub Issue 반영, 그리고 각 경로를 알려달라.
**응답 요약:**
- `worker/worker.js`에 `POST /cost/generate`를 추가해 `단가대비표`, `내역서`, `집계표` Excel 3개를 내부 비밀번호로 접수하도록 했다.
- 접수된 Excel은 R2의 `cost-requests/<repo>/<issue>/<requestId>/...` 경로에 저장하고, GitHub Issue #41에 `원가계산서 생성 요청 접수` 코멘트를 남기도록 했다.
- `index.html`의 `원가계산서 만들기` 패널에 처리 비밀번호와 템플릿 선택을 추가하고, `생성 요청 접수` 버튼이 Worker API를 호출하도록 연결했다.
- `worker/README.md`에 API 경로, form 필드, R2 저장 경로, 응답 형식을 문서화했다.

## Quartz 원가계산서 생성기 적용 및 로컬 저장
**요구:** 새 URL을 만들지 말고 `https://quartz-kiba.pages.dev/` 안에 적용하고, R2 저장뿐 아니라 다운로드 버튼을 누를 때 로컬 저장 위치를 지정할 수 있게 해달라.
**응답 요약:**
- Quartz 지식베이스의 `content/notes/cost-statement-generator.md`에 원장님용 업로드 화면을 만들고, Issue #41 페이지에는 바로가기 콜아웃을 추가했다.
- Worker 허용 출처에 `https://quartz-kiba.pages.dev`를 추가해 Quartz 화면에서 기존 `POST /cost/generate` API를 호출할 수 있게 했다.
- Quartz 공통 스크립트에 File System Access API(`showSaveFilePicker`) 기반 저장 위치 선택 동작을 추가했다. 지원하지 않는 브라우저는 일반 다운로드로 전환한다.
- 사용자 화면은 `https://quartz-kiba.pages.dev/notes/cost-statement-generator`, 진행 기록은 `https://quartz-kiba.pages.dev/issues/issue-41`로 구분했다.
