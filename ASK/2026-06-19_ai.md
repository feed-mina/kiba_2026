# 2026-06-19 Claude/Codex 질문·응답 로그

> 작성 규칙: 질문마다 한 블록. 질문 한 줄 요약 + 응답 2~3줄 요약.
> 도구 구분: `Claude`, `Codex`, 또는 `Claude+Codex`.

---

## GitHub Projects v2 권한 및 ASK/Todo Codex 자동 업데이트
**도구:** Codex
**질문:** GitHub Projects v2 보드 생성 권한(`read:project`/`project`) 부여 방법을 정리하고, ASK/Todo Codex 자동 업데이트를 만들어줘.
**답변 요약:**
- `ADD_TO_PROJECT_PAT`가 `Bad credentials` 상태이고, 로컬 `gh` 토큰에는 `read:project` scope가 부족한 것을 확인했다.
- `.github/PROJECTS_V2_SETUP.md`에 권한 갱신, PAT 생성, secret/variable 설정, Project backfill 실행 방법을 정리했다.
- `todo-reflect.yml`이 `ASK/**` 변경에도 실행되도록 보강하고, Pages 반영 push 경합을 `git pull --rebase origin main` retry로 회복하도록 수정했다.
- `project-backfill.yml`을 추가해 토큰 교체 후 기존 open issue를 Project v2 보드에 일괄 추가할 수 있게 했다.

## Todo/ASK GitHub Issue 및 commit/push 업데이트
**도구:** Codex
**질문:** Todo/ASK 내용을 GitHub Issue와 커밋·푸시까지 업데이트해달라.
**응답 요약:**
- 루트 저장소 상태를 확인하고, `Todo/` 변경 시 `todo-reflect` GitHub Actions가 Todo 파일을 Issue와 Pages 보드에 반영하는 구조임을 확인했다.
- 로컬 `GITHUB_TOKEN/GITHUB_REPOSITORY` 환경 변수는 없고, portable `gh`의 저장 토큰은 만료되어 로컬 직접 이슈 갱신은 불가했다.
- 오늘자 ASK/Todo 로그를 추가하고, 다음 push 후 GitHub Actions가 이슈 반영을 수행하도록 정리했다.

## ADD_TO_PROJECT_PAT 시크릿 교체 및 Project backfill 성공
**도구:** Claude
**질문:** project-backfill 워크플로가 실패한다. 토큰 시크릿을 교체하고 backfill을 돌려달라.
**응답 요약:**
- 실패 원인을 3단계로 진단: (1) `ADD_TO_PROJECT_PAT` 빈 값 → (2) `--body -`로 값이 `-`로 저장돼 `Bad credentials` → (3) Classic 토큰에 `read:org`/`read:discussion` scope 부족으로 `gh project item-add`가 `unknown owner type` 실패.
- fine-grained PAT는 user Projects 쓰기가 막혀(`Resource not accessible`) **Classic PAT**(`repo`,`project`,`read:org`,`read:discussion`)로 전환, stdin으로 시크릿 설정.
- `project-backfill.yml` 성공 → open 이슈 22건이 Project #3에 등록(아이템 24건). 새 이슈(#26)로 `add-to-project.yml` 자동 추가도 검증.
- 노출된 토큰 폐기·재발급 후 시크릿 재교체까지 마무리. (메모: Classic+read:org 필요, fine-grained 불가)

## Todo 보드 "끝난 일" 아코디언 분리
**도구:** Claude
**질문:** 완료된 항목은 활성 Todo에서 빼서 "끝난 일"로 옮기고, 그 UI를 아코디언으로 해달라.
**응답 요약:**
- `scripts/reflect_todo.py`를 수정: 체크리스트가 전부 완료(`done==total`)된 Todo 파일을 활성 보드에서 빼고 접이식 `<details class="fold">` "끝난 일" 섹션으로 모음.
- 기존 `.fold` 스타일 재사용, 미리보기로 접힘/펼침 동작 검증 후 main push → `todo-reflect`로 보드 재생성.

## #24 완료·#13/#10 후속 정리
**도구:** Claude
**질문:** #24(Projects v2 자동화)·#13(스케줄러 후속) 방향 + #10(Pages·Issue 운영) 마무리.
**응답 요약:**
- #24: 오늘 토큰·backfill·add-to-project·todo-reflect 검증으로 13/13 완료 → 끝난 일 이동.
- #13: deploy-worker success·`/docs/list` 200 확인, 18:00 Windows 트리거는 Codex 17:55와 중복이라 미등록 결정. DPAPI 비밀번호 재생성은 사용자 PC 작업으로 남김.
- #10: `todo`/`doing`/`done` 라벨 체계 확립·#1~5 부여, 끝난 일 아코디언 기준, worker 배포 시크릿/Actions 확인, PR #6 close. **#7 검증 중 Worker `GITHUB_TOKEN` 만료 발견**(/counts=0 vs 실제 코멘트 3) → `wrangler secret put` 재설정 필요. Cloudflare 대시보드·팀 공유는 사용자 작업으로 남김.
