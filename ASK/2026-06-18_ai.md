# 2026-06-18 Claude/Codex 질문·응답 로그

> 작성 규칙: 질문마다 한 블록. 질문 한 줄 요약 + 응답 2~3줄 요약.
> 도구 구분: `Claude`, `Codex`, 또는 `Claude+Codex`.

---

## 스케줄러 실행 여부 점검 및 누락분 보충 실행
**도구:** Codex
**질문:** 어제와 오늘 스케줄러에서 안 돈 부분이 있는지 확인하고, 오늘 09:00 404 실패 이유와 어제 빠진 실행분을 같은 스케줄로 보충 실행해달라.
**응답 요약:**
- Windows 작업 스케줄러 `KIBA Docs Download`는 오늘 09:00에 실행됐지만 `/docs/list` 호출이 404로 실패했음을 확인. 로컬 Worker 코드에는 `/docs/list` 라우트가 있으므로, Cloudflare에 최신 Worker가 배포되지 않았거나 해당 라우트가 배포본에 없는 상태로 판단.
- 어제 빠진 18:00 실행분은 다운로드 404를 피하기 위해 `.\scripts\download_docs_scheduled.ps1 -SkipDownload`로 보충 실행. ASK/Todo 변경분을 `chore: ASK/Todo logs 2026-06-18 09:11` 커밋으로 main에 push했고, docs/ASK/Todo를 R2에 동기화.
- 현재 등록된 Windows 스케줄은 09:00, 13:00, 17:50 세 개뿐이며, `setup_docs_schedule.ps1`에 적힌 18:00 트리거는 실제 작업 스케줄러에는 아직 등록되어 있지 않음.

---

## Codex ASK/Todo 자동 기록 스케줄 추가
**도구:** Codex
**질문:** 현재 ASK/Todo 자동 git 업로드가 Claude 쪽 질문만 반영하는데, Codex 쪽 작업도 자동으로 기록·실행되게 해달라.
**응답 요약:**
- Codex 앱 자동화 `daily-codex-ask-todo-log`를 생성해 매일 17:55에 `C:\Users\User\Desktop\KIBA`에서 실행되도록 설정.
- 자동화는 Codex 작업 내용을 `ASK/YYYY-MM-DD_ai.md`에 누적하고 필요한 Todo 파일을 갱신한 뒤, `ASK`와 `Todo` 변경만 commit/push하도록 구성.
- Git 반영 후 `scripts/download_docs_scheduled.ps1 -SkipDownload`를 실행해 다운로드 404를 피하면서 ASK/Todo/docs 백업·동기화를 수행하도록 설정.

---

## 엔지니어링 협회 등록 인력요건 검토 실행
**도구:** Codex
**질문:** `[인력 관리] 엔지니어링 협회 등록을 위한 자격증 및 요건 검토` 작업을 실행해달라.
**응답 요약:**
- `quali-fit/app.db`에서 인력·학력·자격 데이터를 확인하고, 실명 공개 없이 후보군/데이터 보강 상태를 집계했다.
- 경력연수 데이터가 DB에 없어 최종 등급과 최소 10명 요건 충족 여부는 확정할 수 없다고 판단하고, `docs/engineering_registration_review_2026-06-18.xlsx` 비공개 검토 파일을 생성했다.
- Todo 실행계획을 갱신하고, 다음 단계로 엔지니어링 경력연수·사업관리/PM 경력연수·증빙 보유 여부 입력 후 2차 판정을 진행하도록 정리했다.

---

## 별표 5 기준 건설사업관리 후보 10명 이상 분류
**도구:** Codex
**질문:** Issue #1 작업을 `app.db`와 `[별표 5] 건설엔지니어링업 등록요건 및 업무범위` PDF를 참고해, 40명 중 사업관리(건설사업관리) 등록 대상자 10명 이상 후보로 분류해달라.
**응답 요약:**
- 별표 5에서 건설사업관리 요건이 “특급기술인 1명을 포함한 초급 이상의 건설기술인 10명 이상”임을 확인하고, `app.db`의 자격·학력·부서 데이터를 기준으로 후보군을 A/B/C/D로 분류했다.
- 건설 관련 기술사·기사, 건설사업 부서, PM/CMP 자격, 공학·건축·토목 학력을 점수화해 내부 검토용 상위 15명 후보표를 생성했다.
- 비공개 산출물 `docs/construction_engineering_business_management_candidates_2026-06-18.xlsx`를 만들었고, 최종 확정에는 건설기술인협회 등록등급·건설사업관리 경력연수·경력증명서 확인이 필요하다고 정리했다.

---

## 견적서 생성기 exe 빌드 단계별 상세 안내
**도구:** Claude
**질문:** `pip install` → PyInstaller 빌드가 자동 진행되는 부분부터 어떻게 하는지 자세하게 알려달라.
**응답 요약:**
- `build.bat` 더블클릭이 [1] Python 확인 → [2] `pip install`(openpyxl·pyinstaller) → [3] 이전 산출물 제거 → [4] PyInstaller `--onefile --noconsole` 빌드의 4단계를 자동 수행하며, 각 단계 화면 로그와 성공 메시지(`Successfully installed`, `completed successfully`)를 안내.
- 사내망 설치 실패 시 `--proxy` / `--trusted-host` 옵션, 결과물은 `dist\견적서생성기.exe`(15~25MB) 한 파일만 배포하면 받는 PC에 Python 불필요.
- 흔한 문제 대처: SmartScreen 경고("추가 정보→실행"), 백신 오탐(`--noupx`), 무반응 시 `--noconsole` 제거로 에러 확인, 한글 깨짐 `chcp 65001`.

---

## 스케줄러 점검 + Cloudflare Worker 미서비스 복구
**도구:** Claude
**질문:** 어제 스케줄러에서 안 돈 부분이 있는지 확인. 추가로 오늘 자동 스케줄 실행됐는지 확인.
**응답 요약:**
- 어제(06-17) GitHub Actions는 모두 정상(취소 2건은 concurrency 정상 동작), 단 `deploy-worker`가 success 로그를 남겼지만 Worker가 빈 404로 미서비스 상태였음을 발견 → 재배포로 복구.
- 오늘 점검: `KIBA Docs Download`가 09:00/13:00 실행됐으나 실패(0x1) — 원인은 Worker 라우트가 또 죽어 `/docs/list` 404. `workers_dev=true`가 커밋돼 있는데도 재발(00:26 200→14:00 빈 404)함을 확인하고 `npx wrangler deploy`로 복구(전파 15~20초). `KIBA SW Guide Watch`는 09:10 이후 등록돼 오늘 자동 실행은 없고 내일부터, `watch-sw-guide`(Actions)는 cron보다 늦게 추가돼 오늘 스케줄 실행 없음.

## 우선순위 매트릭스 "네트워크 오류" 해결
**도구:** Claude
**질문:** 업무 우선순위 매트릭스가 네트워크 오류로 실패. 관리 비밀번호도 기억 안 남.
**응답 요약:**
- 빈 404 분석으로 Worker 라우트 미서비스 확인 → 재배포로 복구. `DOCS_PASSWORD`는 조회 불가(시크릿)라 `wrangler secret put`으로 재설정.
- 라벨 적용 `github_error`(403)는 Worker의 옛 `GITHUB_TOKEN`에 Issues 쓰기 권한이 없던 것 → 새 fine-grained PAT 재등록으로 해결. 노출된 토큰은 폐기 안내.

## SW 대가 산정 가이드 자동 모니터링 + quali-fit 연동 (Issue #3)
**도구:** Claude
**질문:** sw.or.kr(cbIdx=276) 업데이트 시 quali-fit·KIBA 페이지에서 알 수 있게. 우선 페이지 반영부터.
**응답 요약:**
- KIBA 헤더에 quali-fit 바로가기 단추 추가(배포 완료). `watch_sw_guide.py`로 게시판 변경 감지 → Issue #3 코멘트+라벨 + `data/sw_guide_latest.json` 공개 피드(quali-fit가 fetch).
- 수집기 이원화: 사무실 PC(한국 IP, 매일 09:10, 주 수집기) + GitHub Actions(09:30, 보조; 협회가 해외 IP 차단 시 graceful skip). quali-fit(Streamlit) manage 화면 배너는 `feat/sw-guide-banner` PR로 준비. 라이브 코멘트 테스트로 전 경로 검증 완료.

## Cloudflare Worker 헬스 워치독 생성
**도구:** Claude
**질문:** Worker 라우트가 반복적으로 죽으니 워치독을 만들어달라.
**응답 요약:**
- `scripts/worker_healthcheck.ps1`: `/health`가 200이 아니면(빈 404 등) `npx wrangler deploy`로 자동 재배포 후 20초 대기·재확인, `scripts/worker_health.log` 기록.
- Windows 작업 스케줄러 `KIBA Worker Watchdog`를 30분마다 실행하도록 등록(`setup_worker_watchdog.ps1`). 무인 재배포 안정화를 위해 Cloudflare API 토큰(DPAPI, 선택) 지원.

---
