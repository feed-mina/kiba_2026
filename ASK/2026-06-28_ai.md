# 2026-06-28 Claude/Codex 질문·응답 로그

> 작성 규칙: 질문마다 한 블록. 질문 한 줄 요약 + 응답 2~3줄 요약.
> 도구 구분: `Claude`, `Codex`, 또는 `Claude+Codex`.
> (보류 기간 백필: 커밋 기록을 근거로 사후 정리. 2026-07-04 작성)

---

## 회의록 자동화 엔진(STT+요약) 연동 — Issue #5
**도구:** Claude
**질문:** 회의 전사/음성을 받아 자동으로 회의록과 할 일을 만드는 엔진을 붙여줘.
**응답 요약:**
- CLOVA STT + Gemini 요약 엔진을 Worker에 연동해(Issue #5), `/meeting/summarize` 엔드포인트로 회의 전사·음성을 Gemini 회의록으로 변환하도록 구성했다.
- Gemini 모델을 `gemini-2.5-flash`로 고정하고 할 일(todo) 추출·치환 로직을 개선했다.

---

## 원가 DB 20/20 실제값 완성 + 계보 시각화
**도구:** Claude
**질문:** sample_ver1 원가계산서 20개 테이블을 실제값으로 채우고 계보를 시각화해줘.
**응답 요약:**
- 단가대비표 정규화·계보 연결·비율/간접비 ETL을 추가하고 계보 시각화를 붙였다.
- 총원가 메타를 위치 하드코딩에서 라벨·수식 탐지 방식으로 리팩터링했다.
- 메타 3종 적재로 스키마 20/20 전 테이블 실제값을 완성했다.

---

## 샘플 원가계산서 공개 배포 + 보드 UI 정리
**도구:** Claude
**질문:** 생성기 샘플 원가계산서를 공개 배포하고, 보드의 quali-fit UI를 정리해줘.
**응답 요약:**
- (#41) 생성기 샘플 원가계산서를 공개 배포하고 샘플 다운로드 UI를 보강했으며, sample_ver 단일화와 엑셀(sample.xlsx) 다운로드를 추가했다.
- 보드에서 quali-fit UI를 전체 숨김 처리하고, 바로가기를 '개발자 모드'(dev 가든) 버튼으로 교체하면서 원가견적서 로직을 변경했다.
